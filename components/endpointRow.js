export default ({ title, isActive }) => {
  return (
    <div className="endpoint">
      Title
      <style>
        {`
          .endpoint {
            padding: 0px 10px;
          }
        `}
      </style>
    </div>

  );
} 